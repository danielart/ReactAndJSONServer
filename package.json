{
  "name": "peripheralsUtilities",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "json-server": "^0.9.4",
    "react-scripts": "0.8.5"
  },
  "dependencies": {
    "react": "^15.4.2",
    "react-dom": "^15.4.2"
  },
  "scripts": {
    "start": " json-server -p 3001 --watch db.json & react-scripts start & wait",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
